🔹 JavaScript (DOM + Logic) Questions

Implement a counter app (increment, decrement, reset).

Build a to-do list with add/remove/complete functionality.

Create a modal dialog that opens/closes on button click.

Implement a carousel/slider (next, prev, autoplay).

Form validation (check email format, password strength, required fields).

Build a debounced search input (shows results as you type).

Create a stopwatch or timer (start, pause, reset).

Implement a drag-and-drop list.

Accordion/FAQ section (expand/collapse).

Dark mode toggle (switch between light/dark themes).
