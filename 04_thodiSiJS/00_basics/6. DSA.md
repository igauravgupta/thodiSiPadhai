freq[num] = (freq[num] || 0) + 1;
freqMap.set(num, (freqMap.get(num) || 0) + 1);

# JavaScript Data Structures & Algorithms (DSA)

## 1. Stack (LIFO – Last In, First Out)

A stack can be implemented using an array (push & pop).

```js
class Stack {
  constructor() {
    this.items = [];
  }
  push(element) {
    this.items.push(element);
  }
  pop() {
    if (this.isEmpty()) return "Underflow";
    return this.items.pop();
  }
  peek() {
    return this.items[this.items.length - 1];
  }
  isEmpty() {
    return this.items.length === 0;
  }
  size() {
    return this.items.length;
  }
}

// Usage
const stack = new Stack();
stack.push(10);
stack.push(20);
console.log(stack.pop()); // 20
console.log(stack.peek()); // 10
```

---

## 2. Queue (FIFO – First In, First Out)

A queue can be implemented using an array (push & shift).

```js
class Queue {
  constructor() {
    this.items = [];
  }
  enqueue(element) {
    this.items.push(element);
  }
  dequeue() {
    if (this.isEmpty()) return "Underflow";
    return this.items.shift();
  }
  front() {
    return this.items[0];
  }
  isEmpty() {
    return this.items.length === 0;
  }
  size() {
    return this.items.length;
  }
}

// Usage
const queue = new Queue();
queue.enqueue(10);
queue.enqueue(20);
console.log(queue.dequeue()); // 10
console.log(queue.front()); // 20
```

---

## 3. Map (Key-Value Pairs)

JavaScript has a built-in Map.

```js
const map = new Map();
// Add
map.set("name", "Gaurav");
map.set("age", 22);
// Get
console.log(map.get("name")); // Gaurav
// Check
console.log(map.has("age")); // true
// Iterate
for (const [key, value] of map) {
  console.log(key, value);
}
```

---

## 4. Sorting

JavaScript has `.sort()` for arrays.

```js
const numbers = [5, 2, 9, 1, 7];
// Ascending
numbers.sort((a, b) => a - b);
console.log(numbers); // [1,2,5,7,9]
// Descending
numbers.sort((a, b) => b - a);
console.log(numbers); // [9,7,5,2,1]
```

Sorting strings:

```js
const names = ["John", "Alice", "Bob"];
ChatGPT said:
console.log(names); // [ 'Alice', 'Bob', 'John' ]
```

---

## 5. Reverse

Reverse an array easily:

```js
const arr = [1, 2, 3, 4, 5];
console.log(arr.reverse()); // [5,4,3,2,1]
```

Reverse a string:

```js
const str = "hello";
const reversed = str.split("").reverse().join("");
console.log(reversed); // "olleh"
```

---

## 6. Hashing (Using Objects / Map)

JavaScript objects or Map can act as hash tables.

**Example: Count frequency of elements**

```js
const arr = [1, 2, 2, 3, 1, 4, 2];
const freq = {};
for (const num of arr) {
  freq[num] = (freq[num] || 0) + 1;
}
console.log(freq); // { '1': 2, '2': 3, '3': 1, '4': 1 }
```

**Using Map for hashing:**

```js
const freqMap = new Map();
arr.forEach((num) => {
  freqMap.set(num, (freqMap.get(num) || 0) + 1);
});
console.log(freqMap); // Map { 1 => 2, 2 => 3, 3 => 1, 4 => 1 }
```

---

✅ With these, you can handle stack, queue, map, sorting, reverse, and hashing in JavaScript.

Here’s a clean README.md version with explanations and code snippets for Stack, Queue, Map, Sorting, Reverse, and Hashing in JavaScript 👇

Data Structures & Algorithms in JavaScript

This document provides implementations and usage examples of Stack, Queue, Map, Sorting, Reverse, and Hashing in JavaScript.

📌 1. Stack (LIFO – Last In, First Out)

A stack can be implemented using an array (push & pop).

class Stack {
constructor() {
this.items = [];
}

push(element) {
this.items.push(element);
}

pop() {
if (this.isEmpty()) return "Underflow";
return this.items.pop();
}

peek() {
return this.items[this.items.length - 1];
}

isEmpty() {
return this.items.length === 0;
}

size() {
return this.items.length;
}
}

// Usage
const stack = new Stack();
stack.push(10);
stack.push(20);
console.log(stack.pop()); // 20
console.log(stack.peek()); // 10

📌 2. Queue (FIFO – First In, First Out)

A queue can be implemented using an array (push & shift).

class Queue {
constructor() {
this.items = [];
}

enqueue(element) {
this.items.push(element);
}

dequeue() {
if (this.isEmpty()) return "Underflow";
return this.items.shift();
}

front() {
return this.items[0];
}

isEmpty() {
return this.items.length === 0;
}

size() {
return this.items.length;
}
}

// Usage
const queue = new Queue();
queue.enqueue(10);
queue.enqueue(20);
console.log(queue.dequeue()); // 10
console.log(queue.front()); // 20

📌 3. Map (Key-Value Pairs)

JavaScript has a built-in Map.

const map = new Map();

// Add
map.set("name", "Gaurav");
map.set("age", 22);

// Get
console.log(map.get("name")); // Gaurav

// Check
console.log(map.has("age")); // true

// Iterate
for (const [key, value] of map) {
console.log(key, value);
}

📌 4. Sorting

JavaScript provides .sort() for arrays.

const numbers = [5, 2, 9, 1, 7];

// Ascending
numbers.sort((a, b) => a - b);
console.log(numbers); // [1,2,5,7,9]

// Descending
numbers.sort((a, b) => b - a);
console.log(numbers); // [9,7,5,2,1]

Sorting strings:

const names = ["John", "Alice", "Bob"];
names.sort();
console.log(names); // [ 'Alice', 'Bob', 'John' ]

📌 5. Reverse

Reverse an array:

const arr = [1, 2, 3, 4, 5];
console.log(arr.reverse()); // [5,4,3,2,1]

Reverse a string:

const str = "hello";
const reversed = str.split("").reverse().join("");
console.log(reversed); // "olleh"

📌 6. Hashing

JavaScript objects or Map can act as hash tables.

👉 Count frequency of elements using object:

const arr = [1, 2, 2, 3, 1, 4, 2];
const freq = {};

for (const num of arr) {
freq[num] = (freq[num] || 0) + 1;
}

console.log(freq); // { '1': 2, '2': 3, '3': 1, '4': 1 }

👉 Using Map:

const freqMap = new Map();
arr.forEach(num => {
freqMap.set(num, (freqMap.get(num) || 0) + 1);
});

console.log(freqMap); // Map { 1 => 2, 2 => 3, 3 => 1, 4 => 1 }

✅ Summary

Stack → Use push() and pop() → Example: Undo feature

Queue → Use enqueue() and dequeue() → Example: Task scheduling

Map → Store key-value pairs efficiently → Example: Caching

Sorting → Use .sort() with comparator functions

Reverse → Use .reverse() for arrays and .split("").reverse().join("") for strings

Hashing → Use objects or Map to count frequencies and store quick lookups
